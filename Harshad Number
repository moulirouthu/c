/*harshad number or not*/

#include<stdio.h>
void main() 
{
    int n,r,sum=0,div=0,temp=0;
    printf("enter a number:");
    scanf("%d",&n);
    temp=n;
    while(n>0)
    {
        r=n%10;
        sum=sum+r;
        n=n/10;
     }
    div=temp%sum;
    if(div==0)
      printf("%d is a Harshad Number",temp);
      else
      printf("%d is not a Harshad Number",temp);
}



Output1:
     enter a number:156
156 is a Harshad Number


Output 2:
enter a number:22
22 is not a Harshad Number
